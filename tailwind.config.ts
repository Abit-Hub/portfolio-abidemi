import type { Config } from 'tailwindcss'

const config: Config = {
  theme: {
    extend: {
      colors: {
        dark: '#0D1321',
        light: '#F0EBD8',
      },
    },
  },
}

export default config;